# 开发笔记

> 记录我在开发 StudyOrganizer 过程中的经验、踩过的坑和解决方法。
> 每解决一个难题，就在末尾追加一条。

## 1. 读文本时编码不统一（UnicodeDecodeError）

**遇到的问题**
- 不同文件编码不一样：有的存成 UTF-8，有的存成 GBK。
- 之前写死 `encoding="utf-8"`，一遇到 GBK 文件就报错崩溃：
  `UnicodeDecodeError: 'utf-8' codec can't decode byte ...`

**根本原因**
- 文件在硬盘上存的是一串字节（0101），不是文字。
- 要把字节变回文字，必须先知道它当初是用什么编码写的。

**解决方法（三步）**
1. 用 `rb` 二进制模式读，先拿到原始字节（`b` = binary，别急着翻译）；
2. 用 `charset_normalizer.from_bytes(原始字节)` 自动检测编码；
3. 按检测到的编码把字节解码成文字。

**核心道理**
- 不告诉程序文件是什么编码，它只能瞎猜，猜错就崩溃。
- 所以顺序是「先读字节 → 自动检测 → 再解码」。

**相关代码**：`studyorganizer/extract.py` 里的 `_read_plain_text()`

## 2. 单元素元组要加逗号（sqlite 报 "Incorrect number of bindings"）

**遇到的问题**
- 用 sqlite3 执行带 `?` 占位符的 SQL 时，报错：
  `sqlite3.ProgrammingError: Incorrect number of bindings supplied. The current statement uses 1, and there are 4 supplied.`

**根本原因**
- 写的是 `(f"%{keyword}%")`，括号里只有一个元素且没加逗号，Python 直接把它当成字符串本身，而不是元组。
- sqlite3 拿到字符串 `"%讲义%"`，把它当成 4 个字符（`%`、`讲`、`义`、`%`），于是「1 个占位符对上了 4 个值」。

**解决方法**
- 单元素元组必须加逗号：`(f"%{keyword}%",)`。

**核心道理**
- `(x)` 就是 `x`，`(x,)` 才是元组。括号里只有一个元素时，**逗号决定它是不是元组**。

**相关代码**：`studyorganizer/store.py` 里的 `search_files()`（`SELECT ... WHERE title LIKE ?`）

## 3. 持久数据 vs 会话状态（网页一进去就显示旧方案）

**遇到的问题**
- 网页打开时，整理方案区自动显示了旧的建议清单，但用户还没点"生成"。

**根本原因**
- `plan_items` 存在数据库里，是"持久"的，之前测试留下的数据被读出来了。
- 数据库的数据程序关了还在；普通变量每次重跑/重启会被重置。

**解决方法**
- 用 `st.session_state` 记住"本会话是否点过生成"，点过才显示。
- `st.session_state.get("show_plan")` 安全取值（键不存在时返回 None，不报错）。

**核心道理**
- 数据库 = 持久，跨会话都在；变量 = 临时，重跑就没了。
- Streamlit 每次交互都重跑脚本，普通变量会丢，`st.session_state` 能跨重跑保留。

**相关代码**：`app.py` 的整理方案部分

## 4. 导入慢：把重的库留到「用的时候」再 import

**遇到的问题**
- 跑 `pytest` 要 5 秒多，可测试本身几乎不耗时——全是些判断和比较，真正跑起来不到 1 秒。
- 怪的是：这些测试**根本没用语义模型**，那时间花在哪了？

**根本原因**
- 写在文件最顶上的 `import`，在这个模块**被导入的那一刻**就会执行。
- `sentence_transformers` 会连带把 `torch` 一起拉进来，那是个很大的库，光 import 就要好几秒。
- 测试文件开头写了 `from studyorganizer.search import ...`，于是光是这一行就付了这笔钱——
  哪怕测试压根不碰模型。

**解决方法（延迟导入）**
- 把它从文件顶部**移进真正用到它的那个函数里**：

```python
def _get_model():
    global _model
    if _model is None:
        from sentence_transformers import SentenceTransformer   # 用到了才 import
        _model = SentenceTransformer(_MODEL_NAME)
    return _model
```

- 效果：测试从 **5.4 秒 → 1.5 秒**；`import studyorganizer.search` 之后 `torch` 也确实没被加载
  （可以用 `sys.modules` 验证：`'torch' in sys.modules` 是 `False`）。

**核心道理**
- **写在顶部的 `import` 是「不管用不用都要付的钱」；写进函数里是「用了才付」。**
- 判断标准：这个库是不是**又重、又不是每次都用**？是 → 就延迟导入。
- 不用担心会变慢：`import` 有缓存，第二次执行几乎零开销。
- 但别无脑用：轻量的库（`os`、`json`）放顶部更清楚。到处延迟导入会让人**看不出这个模块依赖什么**。

**相关代码**：`studyorganizer/search.py` 的 `_get_model()`（现已改名 `get_model()`）

## 5. 循环导入：让「建索引」和「检索」互相看得见，但只准单向依赖

**遇到的问题**
- 做向量持久化时，需要有个地方回答「库里哪些文件的向量过期了」。
- 直觉做法是写在 `search.py` 里（它本来就是管向量的），可算完向量要**存库**，
  那就得 `import store`；而 `store.py` 的 `import_folder` 又想导入完顺手建索引，
  于是要 `import search`——两边互相 import，转一圈回来了。

**根本原因**
- Python 导入一个模块时，会从头到尾执行它。A 执行到一半需要 B，
  就去执行 B；B 执行到一半又需要 A，可 A 还没执行完（里面那个名字还不存在），
  于是报 `ImportError: cannot import name ... (most likely due to a circular import)`。
- 根子上是**职责没分清**：`store` 只该管「怎么存」，不该管「什么时候该建索引」。

**解决方法（加第三个模块，把依赖捋成一条直线）**
- 新建 `studyorganizer/index.py`，专门放「建索引」这件事：

```
index.py  →  store.py    （读要算的、写算完的）
index.py  →  search.py   （借模型和模型名）

store.py  →  不 import index，也不 import search
search.py →  不 import index
```

- 关键点：**`index` 站上层，`store` 和 `search` 都待在下面。**
  底下的模块不认识上面的模块，箭头就永远转不回起点，循环自然断了。
- 为此把 `search.py` 的 `_get_model` / `_MODEL_NAME` 改成了公开的
  `get_model` / `MODEL_NAME`——`_` 开头是「本模块内部用」的意思，
  别的模块要用它，本来就该改成公开名字。

**核心道理**
- **两个模块互相 import，是「职责放错了」的信号，不是「import 写法不对」。**
  先别研究怎么让它编过，先问：这件事到底该谁管？
- 断循环的正经办法是**加一层**（谁依赖谁画成箭头，别绕圈），
  不是把 import 挪进函数里藏起来——那只是把错误推迟到运行时，更难查。
- 单向依赖还有个白送的好处：**谁也不能偷偷在建索引**。
  要建索引只能从 `index` 或界面层调，看一眼 import 就全知道了。

**相关代码**：`studyorganizer/index.py`（新建）、`studyorganizer/store.py` 的
`list_files_needing_embedding()` / `save_embedding()`、`studyorganizer/search.py` 的 `get_model()`

## 6. PDF 提取出来的「段落」，其实是一整页

**遇到的问题**
- 做段落级向量，切段规则是「按空行分段」。拿 practice/ 里的 txt 试，切得挺好，
  一个自然段一段。
- 换成 PDF 一试，切出来每一「段」都是几百字的一大坨——因为**一页就是一「段」**。
  按这个切，段落级向量等于没做。

**根本原因**
- PDF 文件里**根本没有「段落」这个东西**。它存的是一页一页的排版结果：
  每个字画在哪个坐标上。至于"这两行之间空了一行、所以是新段落"，
  那是人看版式看出来的，文件里没有任何标记。
- PyMuPDF 的 `page.get_text()` 只能把同一页的文字按视觉行读出来，行与行之间给个换行。
  段落之间那个视觉空当，读出来什么都不剩。
- 所以提取出来的正文里，唯一会出现**空行**的地方是**页与页的接缝**：
  一页的结尾本来就有换行，拼接时又补了一个，两个撞在一起就成了空行。
  实测：3 页 PDF → 2 个空行，正好等于「页数 - 1」。

**解决方法**
- 切法加第二层：某一段超过 300 字，就按字数滑窗再切开（相邻块重叠 50 字）。
  对 txt 来说这层平时用不上，对 PDF 来说这层才是主力。
- 另外把切法的第一层也补严了：**单个换行不算分段，要拼回去**。
  因为 PDF 是「每一视觉行一个换行」，不拼回去的话每一段就变成 40 字一行了。

**核心道理**
- 从别的格式里"提取纯文本"，一定会丢掉一部分结构——那些**排版表达得了、
  纯文本表达不了**的东西。段落就是典型：它活在视觉间距里，不在文字流里。
- 所以别指望提取出来的换行还带着原文的分段含义。既然结构丢了，
  就得**自己按长度兜底**，不能只依赖原文的标记。
- 顺带一条：判断标准是"最坏情况下会怎样"，不是"我手上这几个文件跑得通"。
  txt 上验证通过不代表 PDF 上成立——语料里有什么格式，就得拿什么格式试。

**相关代码**：`studyorganizer/chunk.py`、`studyorganizer/extract.py` 的 `_read_pdf()`

## 7. SQL 的 trim() 只去空格，不去换行

**遇到的问题**
- 写了个查询，挑出「还没切过段的文件」，条件是 `trim(text) != ''`
  （正文是空的就没什么可切的，直接跳过）。
- 结果有个文件每次都被拎出来重新切一遍。而重新切又切不出任何段落，
  库里永远不会出现它的记录，于是**下次又满足条件**——永远循环下去。

**根本原因**
- SQLite 的 `trim(X)` 默认**只去掉空格**，不去换行、也不去制表符。
  `trim('  \n  ')` 得到的是 `'\n'`，**不等于空字符串**。
- 我脑子里想的是「把空白都去掉」，实际拿到的是「把空格去掉」。
- 这个 bug 是**测试抓出来的**：我原本以为"正文为空"只有扫描件那一种情况
  （提取出来是真正的空串）。是测试里随手写的 `"   \n  "` 把它暴露了。

**解决方法**
- 把判断从 SQL 挪到 Python 里做，用 `str.strip()`——它去掉的才是**全部**空白字符：

```python
rows = conn.execute("SELECT id, text FROM files WHERE id NOT IN (...)").fetchall()
return [(fid, text) for fid, text in rows if text and text.strip()]
```

**核心道理**
- **同一个名字的函数，换个语言行为就可能不一样。**「trim / 去空白」这种听起来
  毫无歧义的词，SQL 和 Python 就不同——SQL 要写 `trim(X, ' \t\n\r')` 才是全去。
- 跨语言边界传判断逻辑时留个心眼：能挪到「你看得懂、能直接验证」的那一边，就挪过去。
  这里 SQL 只负责取数据，怎么算"空"交给 Python 判断，一眼就能看出对不对。
- 还有个小收获：`text and text.strip()` 顺手把 `None` 也挡了。
  SQL 里的 `trim(NULL)` 返回 `NULL`，`NULL != ''` 也**不是**真值，
  这类三值逻辑很容易写错，挪到 Python 就没这问题。

**相关代码**：`studyorganizer/store.py` 的 `list_files_without_chunks()`

## 8. 评测模型时，标准答案要读语料确认，不能凭文件名猜

**遇到的问题**
- 换向量模型要比较几个模型的区分度，我给四条测试查询各标了一份「标准答案」，
  其中一条是「动态规划的状态定义怎么写」→ 标准答案填了 `算法_动态规划长讲义.txt`。
- 一量，text2vec 和 bge-small 在这条上都把标准答案排到第 2、第 3 名，
  排在前面的是 `讲义1.txt`、`笔记1.txt`、`笔记2.md`。我差点把这写成"模型分不清"。

**根本原因**
- 我去读了那几个文件，发现 **`笔记1.txt` 里明写着「重点：状态的定义和状态转移方程」，
  `笔记2.md` 里明写着「1. 定义状态：明确 dp 数组的含义」**——它们才是更贴题的标准答案，
  是**我标漏了**，不是模型错了。
- 更根本的原因：标准答案是我**看着文件名**填的。`笔记1.txt` / `讲义1.txt` 这种名字
  完全看不出内容，我就想当然地以为讲得最全的「长讲义」才是答案。
- 还有一个陷阱：`practice/` 里 22 个文件中有 4 个都在讲动态规划。这条查询在这份语料上
  **本来就分不出高低**，拿它当评测题是设计得不好。

**解决方法**
- 把这条查询从对照表里去掉，并说明原因（不是悄悄删掉）。
- 原来的结论（间距 -0.040「模型分不清」）作废，按剩下的三条干净查询重新下结论。

**核心道理**
- **评测里最危险的错误不是算错，是标错。** 算错会报错或对不上；标错会安静地
  产出一个看起来很有道理的错误结论，然后拿着它去改代码。
- 定标准答案时要**打开文件看内容**，不能凭文件名和直觉。这一步花的时间，
  比后面基于错结论折腾的时间少得多。
- 实测完一个指标后，如果结论是「模型有毛病」，先反问一句：**会不会是我的测试写错了？**
  尤其是当所有被测对象（这里四个模型全都"错"）表现一致的时候——
  一个让所有人都错的测试，大概率错在测试本身。
- 顺带：**评测题本身也要挑**。同一份语料里有一堆主题重复的文件时，
  挑一条分不出高低的查询，量出来的数字没有意义。

**相关代码**：无（这是评测方法上的坑）。结论记在 DESIGN.md 的模型对照表里。

## 9. 「版本对不上要作废」这个判断，写的时候查了，读的时候也得查

**遇到的问题**
- 换向量模型时，库里存着旧模型（768 维）和新模型（512 维）两种向量。
  正常路径没事，但**重算几十秒，用户等不及 Ctrl-C 中断**，库里就真会两种并存。
- 这时检索直接崩：
  `ValueError: Incompatible dimension for X and Y matrices: X.shape[1] == 512 while Y.shape[1] == 768`
- 讽刺的是这个坑**是我自己上一阶段埋的**：`embedding_model` 那一列就是为「判断过期」
  加的，但只在**写**的路径上用（`list_*_needing_embedding` 查它决定要不要重算），
  **读**的路径完全没查——`list_file_vectors` / `list_chunk_vectors` 只看了
  `embedding IS NOT NULL`。

**根本原因**
- 我把 `embedding_model` 理解成"一面用来触发重算的旗子"，而它其实是
  **"这行数据属于哪个版本"的标签**。旗子只在写的时候有人看；标签是**谁碰这行数据
  都得先核对**的。
- 换个说法：`embedding IS NOT NULL` 只回答了"这行有数据吗"，没回答
  "这行数据**跟当前模型配得上**吗"。这两个问题不一样，我只检查了前一个。

**解决方法**
- 两个读函数都加上 `AND embedding_model IS ?`，并且 `model_name` 定为**必填参数**
  （不是默认 `None`）。必填很关键：默认 `None` 的话，忘了传就会悄悄退化成
  "不过滤"，而这正是要修的那个 bug。必填会当场报错。
- 顺带明确了调用链：`search → store`、`plan → cluster → store`，
  由 `app.py` 传 `search.MODEL_NAME`。
- 效果：半成品状态从「崩溃」变成「暂时读不到」（检索返回空），补跑一次 `ensure_*`
  就恢复。

**核心道理**
- **凡是"这行数据/这段缓存属于哪个版本"的标记，读的时候必须核对，不能只在写的时候用。**
  缓存、模型、格式版本、协议版本，全是同一类问题。
- 判断"能用吗"通常要两个条件：**存在吗** + **对得上吗**。只查存在性是最容易犯的错
  ——它在正常数据上永远成立，所以测试全绿，问题只在异常状态才冒出来。
  写这类查询时，习惯性地问一句：**"这里要不要再加一个版本/来源的条件？"**
- 参数该默认 `None` 还是必填，看"忘了传会发生什么"：忘了传等于**关闭检查** → 必填。

**相关代码**：`studyorganizer/store.py` 的 `list_file_vectors()` /
`list_chunk_vectors()`、`studyorganizer/cluster.py` 的 `cluster_files()`

## 10. 测量脚本要用被测系统自己的口径，别自己另立一套

**遇到的问题**
- 写 `tools/benchmark.py` 来复现 DESIGN.md 里的检索数字（原来那些是临时脚本算的，
  删了，谁也复现不了）。第一版跑出来，6 条测试查询**全部「未上榜」**，一条都没排上名次。
- 可打印结果里 top-1 明明就是正确答案（`算法 动态规划长讲义`）。看着像"检索彻底坏了"，
  或者"标准答案全标错了"。

**根本原因**
- `search_semantic` 返回的是**标题**，不是文件名。`extract` 会把文件名清洗一遍：
  下划线换成空格、去掉扩展名（`算法_动态规划长讲义.txt` → `算法 动态规划长讲义`）。
- 我的标准答案里写的是**文件名**（因为好认、跟磁盘上对得上），却直接拿它去比对
  返回值。`'算法_动态规划长讲义.txt'` 和 `'算法 动态规划长讲义'` 永远对不上，
  于是每条查询都"未上榜"。
- 同一个东西有两个名字，我用错了那个。**错的是量具，不是被测的东西。**

**解决方法**
- 从库里取出 `文件名 → 标题` 的对应关系，比对之前先翻译一道。
- 更关键的第二件事：**让"对不上"直接报错**。现在脚本会检查每个标准答案文件名在语料里
  到底存不存在，有一个对不上就停下并列出名字；如果有两个文件清洗后标题撞车
  （按标题找答案会串台）也会警告。

**核心道理**
- 写测量脚本时，**凡是跨边界取名字，都得显式翻译一道**。磁盘上的文件名、库里的标题、
  界面显示的标签很可能是三个东西，而清洗规则藏在 `extract` 里，不看代码根本想不到。
- 真正该记的是**它失败时的样子**：结果是"0/6，全部未上榜"。这是个**安静**的失败——
  它长得像"没有结果"，不像"出错了"（跟第 9 条里"忘了过滤就悄悄退化成不过滤"同一类）。
  静默出来的错值不值钱，取决于你有没有让它有机会报错。
- 所以"必须对得上才对"的地方，宁可写死检查、让它当场炸，也别让它退化成空结果
  ——空结果会被当成真结论读下去。
- 顺带：这次差点重演第 8 条。看到"全部未上榜"我第一反应是"检索出问题了？"，
  可 top-1 明显是对的——**先怀疑量具**，永远是性价比最高的那一步。

**相关代码**：`tools/benchmark.py`

## 11. 调一个参数之前，先弄清它同时参与了几件事

**遇到的问题**
- 一直在想"要不要把相似度下限 `min_score` 调高一点"，因为直觉上
  "门槛高 = 过滤得更狠 = 更准"。
- 把 21 条查询的阈值扫描跑出来，结果两头都不对劲：
  - 调**高**到 0.65：对了 17/21（答案段落被整条砍掉，直接"未上榜"）；
  - 调**低**到 0.30 / 0.40：**反而只有 20/21**，「过拟合怎么解决」被顶到第 2 名。

**根本原因**
- 我脑子里的模型是"`min_score` 是个过滤器，只决定**丢不丢**"。可它在代码里干了**两件**事：
  1. 丢掉低于它的段落（过滤）；
  2. 剩下的段落按文件分组、**取最好的前 3 段求平均**当文件分（评分口径）。
- 影响排序的是第 2 件事。门槛一放宽，参与平均的段落就变多——多出来的正是那些
  "够格但没那么准"的段落（刚过 0.45 的那些），它们把文件分往下拉。
  拉多拉少各个文件不一样，**排序就跟着变了**。
- 所以这个参数不是"越严越准"的旋钮，它是个**区间**：太低等于没滤（噪音参与平均），
  太高把答案段落也砍了（连参与平均的机会都没有）。
- 一句话：**我以为我在调"丢多少"，其实我还在调"分数怎么算"。**

**解决方法**
- 不再动 `0.45`，并把扫描结果如实写进 DESIGN.md——包括"调低反而更差"这一半。
  原先文档只写了"调高不会更准"，那只是一半的真相。
- 把这条写进脚本的输出里：阈值扫描不只打 `20/21` 这个数，还把**每条没过线的
  查询名字和名次**打出来。原来光看一个数字，不知道该去查哪一条。

**核心道理**
- **改一个参数之前，先问它被用在几个地方。** 只在"我要调的那个用途"里看它，
  一定会漏掉另一处的作用——而排序往往是另一处决定的。
- 判断"调高了更准"这种直觉时，别只做单向实验（只往上调）。**两头都试一遍**，
  才知道它是不是单调的。这次如果只往上调，会得出"太高不行、那就保持现状"，
  依然不知道调低也会坏。
- 顺带修了同一类的另一个坑：这个脚本里原本**自己抄了一份**"当前默认阈值 = 0.45"，
  而真正的默认值写在 `search_semantic` 的签名里。两份数各走各的，哪天改了真值，
  报告还会印着旧数说"<- 当前默认"——**报告自己变成假的**。改成从签名里读
  （`inspect.signature(...).parameters["min_score"].default`）。
  同一个事实只准有一个来源，这不只是"代码整洁"，是量具准不准的问题（第 10 条）。

**相关代码**：`studyorganizer/search.py` 的 `search_semantic()`、
`tools/benchmark.py` 的阈值扫描与 `当前默认阈值`

## 12. 量完了，结论是「不改」——这也是交付

**遇到的问题**
- ROADMAP 上挂着一条待办：「把绝对阈值 `min_score = 0.45` 改成相对阈值」
  （`分数线 = 均值 + k × 标准差`），理由是绝对分数线不可移植、实测分不干净。
- 计划里定好了流程：**先量再改**。第一步只改量具、不动生产代码，量完再决定。

**量之前先补的一件事：让「卖点」变得可测**
- 相对阈值真正的卖点是「**没有答案时应该少返回、最好返回空**」。
- 可原来的 21 条查询**每一条都有标准答案**——量具里根本没有"没有答案"这种查询，
  这个卖点**一条都测不到**。
- 于是先补了 4 条语料外的探针（红烧肉 / 五线谱 / 光合作用 / 唐朝灭亡），
  按第 8 条的规矩**先把 22 个语料文件全读一遍**，确认里面确实没有答案，才敢标。
- 教训：**如果一个改动的卖点你的量具测不到，那你没法判断该不该做它。**
  补量具这一步不是"准备工作"，它本身就是判断的一部分。

**量出来的结果（`tools/benchmark.py` 第四节）**

| 分数线 | 答案排第1 | 有答案平均返回 | **语料外平均返回** |
| --- | --- | --- | --- |
| 绝对 0.45（现状） | 21/21 | 10.9 个文件 | **1.8** |
| 均值 + 0.5×σ | 21/21 | 12.7 | 14.2 |
| 均值 + 1.25×σ | 21/21 | 5.0 | 7.8 |
| 分数最高前 5% | 21/21 | 3.8 | 5.0 |

- 「排第 1」全都只是**打平**，没有一条规则更高；
- 「语料外返回几个文件」**全都更差**——现状 1.8 个，相对线最好也要 5.0 个。
  摆平"有答案返回数"再比更明显：现状 10.9 个文件时语料外只漏 1.8 个；
  相对线要做到 10 个文件（前 20% 是 10.3），语料外要漏 **12.0** 个——差 6~8 倍。

**根本原因：相对线衡量的是「谁比谁高」，不是「谁够格」**
- 拿语料外的「红烧肉怎么炖才软烂」看，它的 141 个段落分数是
  `0.35 0.30 0.28 ... 0.19 ...`。均值 ≈ 0.21，加 0.5 个标准差 ≈ **0.24**。
- 这条线砍掉了 0.24 以下的一半，**但 0.24 以上的那一半全留下了**——全是垃圾，
  只是"垃圾里相对高一点"的那半。
- 关键就在这儿：**一条完全跑题的查询，它的分数照样有高有低**，相对线照样
  砍一半留一半，所以它**永远不可能返回空**。你把「这个问题我没找到相关内容」
  变成了「三个很自信的错文件」。
- 只有**绝对**分数线才能表达「够格」这件事；相对分数线只能表达「相对高低」。
- （理论上想过的补救——加一条绝对"地板"——实测只在 1~3 条查询上生效，救不回来。
  而且地板加得狠一点，就等于又变回绝对阈值了。）

**最后怎么处理的**
- **`search.py` 一行没改**，保持 0.45。把这次的结论写进 DESIGN / ROADMAP / CHANGELOG / 本条。
- ROADMAP 上那条待办**从「改成相对阈值」改写成「量过了，结论是不改」并打勾**，
  数据一并写进去。勾代表的是「这件事有结论了」（含"结论是不做"），
  不是「这个改动做完了」——**不然一个负结果在清单上永远挂成"未完成"，
  下次看到又会重新纠结一遍。** 顺手把真正还没解决的方向单列成一条新待办
  （改「前 3 段平均」的合成规则）。

**核心道理**
- **负结果也是结果。** 花一整天量出来"这么做没用"，和量出来"这么做有效"是等价的
  交付——前提是你把过程和数字写下来，不然它就真的白花了。
- **先量再改**的价值就在这一次：如果直接动手改 `search.py`，会得到一个
  "看起来更高级"的相对阈值，然后**在语料外查询上悄悄变差 6~8 倍，而且没人知道**
  ——因为原来的旗舰指标（答案排第 1）对这个变化完全瞎。
- 一个诚实的保留：绝对线 0.45 是在这 141 个段落上校准的，有主场优势。相对线在
  "换语料 / 换模型、分数整体平移"时理论上更稳，但**这次量具验不了**（要再灌一套语料）。
  所以结论只能写成"在我们唯一能测的场景里它严格更差"，不能写成"相对阈值是错的"。

**相关代码**：`tools/benchmark.py` 第四节（相对阈值扫描）与 `QUERIES` 里的 4 条
`"answers": []` 探针；结论落在 `DESIGN.md`「检索结果怎么从段落合成到文件」一节

## 13. 换了个指标，同一个盲点又犯一次

**遇到的问题**
- 上一轮（第 12 条）发现旗舰指标「答案排第几」是**瞎的**——它看不见"结果列表变短"。
  所以这轮量合成规则时，我特地发明了一个新指标：
  **「最小领先」= 21 条查询里「标准答案分 − 最高的非答案文件分」的最小值**，
  想用它衡量"排序结实不结实"。
- 跑出来最好的格子是「前 3 段平均 @ 阈值 0.60」：最小领先 **0.040**，
  是现状（0.007）的 **6.1 倍**。看着像白捡的大便宜。

**根本原因**
- 摆了第三张表（平均返回文件数）才看穿：0.60 那格之所以领先得那么舒服，
  是因为**「有答案」的查询平均只剩 1.6 个文件**（现状 10.9 个）。
- **把结果删光，第 1 名当然领先。**
- 也就是说，「最小领先」和「答案排第几」是**同一类指标**——都只看**名次**，
  都看不见"分母"（列表里还剩几个东西）。上一轮我以为换个指标就跳出去了，
  其实只是从一个名次指标换到另一个名次指标。
- **我甚至还犯了个更基础的量具错**：第一版「领先」定义成「第 1 名 − 第 2 名」，
  跑出来"最薄的是动态规划和贪心"，一看拆解才发现那条查询有**三份标准答案**，
  第 2 名也是正确答案——量的是两个正确答案之间的差距，跟"会不会被错文件顶掉"
  毫无关系。**这是第 10 条的同类错（量具自己错了），这次错在新写的指标定义上。**

**解决方法**
- 「最小领先」改成对着**最高的非答案文件**量（不是第 2 名）。
- 增加**表 C：平均返回文件数**，并且写死一条读法：
  **任何关于门槛/合成规则的主张都要配表 C 看。** 比较不同门槛时，
  先看"有答案"那一行还在不在 `top_k`（默认 5）附近——低于它，
  就是用户真的看到结果变少了。
- 结论也跟着改了：**不改 `_TOP_N`**。固定 0.45 时「前 5 段平均」确实更好，
  但二维扫描显示 `前 3 @ 0.60 = 0.040` **超过** `前 5 @ 0.60 = 0.028`
  ——阈值调对之后，那个"更好的合成规则"就不需要了。**先看清哪个参数才是杠杆，再决定改谁。**

**核心道理**
- **一个指标盲在哪，往往不在它"量什么"，而在它"不量什么"。** 名次类指标全都
  默认"分母不变"；只要你的改动可能改变分母（返回条数、候选池大小、样本数），
  名次类指标就一定会骗你。
- 验的量具也得验"量具的定义"。这轮的自检（本地重算 vs 真的 `search_semantic`，
  最大差 0.000000）证明了**换算没错**，但它证明不了**指标选得对**——
  这两件事要分开查。算得准 ≠ 量得对。
- 一个有用的副产品：这轮顺手核对出一句**文档里当结论写着、其实从没量过的话**
  （"三个刚过线的错段落平均起来，赢过一段很准的对段落"）**是错的**。
  详见表里那句订正——写文档时的推断，隔几轮就长得像事实了。

**相关代码**：`tools/benchmark.py` 第五、六节（合成规则扫描 + 二维扫描，
以及那行量具自检）；结论落在 `DESIGN.md`「这个合成规则到底在干什么」
