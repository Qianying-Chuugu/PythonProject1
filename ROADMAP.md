# ROADMAP

## 版本规划

- **v0.1（当前阶段）**：本地单机版，核心链路跑通，全部离线，不移动原文件。
- **v0.2**：把核心能力封装为 FastAPI 接口。
- **v0.3+**：接入大模型（如把检索/分类证据交给 LLM 生成说明）、OCR、更多格式等。

## v0.1 目标

实现「导入 → 提取 → 分类打标签 → 建索引 → 检索 → 整理方案」的完整闭环。

## 任务清单

> 勾选标记：[x] 已完成；[~] 部分完成（括号说明已做/未做）；[ ] 未开始。
> 本清单已于 2026-09-13 对照代码对齐，之前部分标记滞后于实际实现。

### 基础设施
- [x] 项目骨架与模块划分（核心包 + Streamlit 壳分离）
- [~] SQLite 表结构（已做 files / plan_items / courses / chunks；tags / file_tags 未做）
- [ ] 日志与可复现性（固定随机种子）

### 文本提取
- [x] TXT / Markdown 提取（含编码探测 charset-normalizer）
- [x] PDF 提取（PyMuPDF）
- [x] 标题提取与文件名清洗
- [x] 扫描件（无文本层 PDF）识别与提示

### 分类
- [ ] 统一分类接口抽象（TypeClassifier）
- [x] 规则分类器（讲义 / 作业 / 试卷 / 笔记 / 实验报告）
- [x] 课程候选建议（CourseSuggester，半自动；规则打底：从文件名抽课程名）
- [~] 用户修改 → 回写数据库（课程已做；标签未做）
- [~] 建议值与用户修正分开落库（课程已做 suggested_course_id / course_id；标签未做）

### 向量与索引
- [x] 文档级向量（用于聚类；已持久化到 `files.embedding`）
- [x] 段落级向量（用于语义检索；`chunk.py` 切段 + 持久化到 `chunks.embedding`）
- [x] 向量持久化与「过期重算」判断（正文变了 / 换模型 → 自动重算，见 DESIGN.md）
- [ ] 固定模型缓存路径（模型已由 sentence-transformers 默认缓存，离线可用；未显式固定路径）
- [x] **换区分度更强的中文模型**。已换成 `BAAI/bge-small-zh-v1.5`（实测见 DESIGN.md）：
      21 条带标准答案的查询里，旧模型只有 15 条把答案排第一，新模型 21/21；
      同样卡 0.45 放行率从 46% 降到 23%，而且体积小 4 倍、编码快 4 倍。
      换模型没写任何迁移代码——`embedding_model` 对不上就自动全部重算，上一阶段铺的路用上了
- [x] 向量读函数也核对 `embedding_model`：`list_file_vectors(model_name)` /
      `list_chunk_vectors(model_name)` 只返回当前模型算的向量，名字从
      `search` / `plan → cluster` 一路透传。换模型重算到一半被打断时，
      原先会崩 `ValueError: Incompatible dimension`（768 混 512），
      现在只是暂时读不到，补跑一次 `ensure_*` 就恢复（实测验证过）。见 DESIGN.md
- [x] **改成相对阈值：量过了，结论是不改。** 用 `tools/benchmark.py` 第四节的
      相对阈值扫描对比了「绝对 0.45」和 6 档 `均值 + k×σ`、4 档「分数最高前 p%」，
      在 25 条查询（21 条有答案 + 4 条**语料外**探针）上：
      - 「答案排第 1」**全是打平 21/21**，没有一条规则更高；
      - 「语料外平均返回几个文件」**全都更差**（基准 1.75 个，相对线最好也要 5.00 个）。
        摆平"有答案返回数"再比更明显：基准 10.9 个文件时语料外只漏 1.75 个，
        相对线要做到 10 个文件（前 20% = 10.3）语料外就要漏 12.00 个——**差 6~8 倍**。
      - 原因是相对线衡量的是「这条查询里**谁比谁高**」，不是「谁**够格**」：
        「红烧肉怎么炖才软烂」在语料外查询里分数也是 `0.35 0.30 0.28 …`，
        均值+0.5σ ≈ 0.24 照样砍一半留一半，**永远不可能返回空**——等于把"没找到"
        变成"三个很自信的错文件"。加绝对地板的补救实测只在 1~3 条查询上生效。
      - 所以这一轮 `search.py` 一行没改，**当时**保持 `0.45`（后来提到 0.50 是另一件事，
        动机是"语料外少返回"，见本清单最后一条）。完整数据见 DESIGN.md「检索结果怎么
        从段落合成到文件」，教训见 NOTES.md 第 12 条。
      - **保留意见**：0.45 是在这 141 个段落上校准的，有主场优势；相对线在"换语料 /
        换模型、分数整体平移"时理论上更稳，但这次的量具验不了（要再灌一套语料）。
- [x] **改「前 3 段平均」这个合成规则：量过了，结论是不改。**
      `tools/benchmark.py` 新增第五、六节（合成规则扫描 + `min_score × 合成规则`
      二维扫描，**都不过模型**——段落分数已在内存里）。结论：
      - 固定 0.45 时，「前 5 段平均」确实满足事先定好的两条标准（排第 1 仍是 21/21，
        最小领先 0.007 → 0.023）。**但二维扫描一摆出来就翻了**：
        `前 3 @ 0.60 = 0.040` **高于** `前 5 @ 0.60 = 0.028`。
        前 5 只是在"阈值偏低、放进太多平庸段落"时的补救；阈值调对了就不需要。
        **`_TOP_N = 3` 不是薄弱环节，阈值才是。**
      - 「只取最高分」20/21（输的那条正是「0-1 背包」）、「全部过线段平均」18/21，
        都在表 A 里当场淘汰。
      - 「0-1 背包」那句流传的说法——"三个刚过线的错段落平均起来，赢过一段很准的
        对段落"——**拆开量了，是错的**：对文件有 20 段过线（前三段 0.750/0.744/0.719），
        不是"一段"；错文件那三段是 0.766/0.677/0.650，只有第一个是尖峰，不是"刚过线"。
        真实机制是**尖峰 vs 深度**，且被独立验证（换「只取最高分」后那个错文件立刻翻上来）。
        文档已按实测订正。
      - 完整表见 DESIGN.md「这个合成规则到底在干什么」，教训见 NOTES.md 第 13 条。
- [x] **把 `min_score` 从 `0.45` 提到 `0.50`：量完了，这次改了。**
      这是本项目第一次"数据支持改、于是真改了"的调参（前两轮量完的结论都是不改）。
      `search.py` 只动了签名默认值这一个数，`_TOP_N` 仍是 3。依据（DESIGN.md 表 A/B/C）：
      - 答案排第 1 仍是 21/21；
      - 最小领先 0.007 → 0.015，最薄那条厚了一倍；
      - **语料外查询平均返回文件数 1.75 → 0.25**（4 条探针里 3 条返回空文件）
        ——上一轮改相对阈值想要却没拿到的效果；
      - 「有答案」查询平均返回 10.9 → 5.0 个文件，但 `search_semantic` 默认
        `top_k` 就是 5，**用户可见的结果一条没少**。
      翻 NOTES.md 第 11 条那句"不再动 0.45"的理由是干净的：语料外那 4 条探针从没参与过
      阈值调参，算半个留出集，而这次动的正是它们。
      **再往上加就不行了**：0.55 → 2.3 个文件、0.60 → 1.6，开始真的少于 `top_k = 5`；
      而 0.30 / 0.40 反而掉到 20/21（门槛同时是"前 3 段平均"的取材范围，NOTES 第 11 条）。
      教训记在 NOTES.md 第 14 条。
- [ ] 段落向量的向量索引（现在每次检索都把全库段落读进内存算余弦；几百~几千段够用，
      上万段就该换 FAISS 之类）

### 检索
- [ ] 统一检索接口抽象（Retriever）
- [x] 文件名关键词检索
- [x] 正文关键词检索（TF-IDF；BM25 未做）
- [x] 语义检索（按段落匹配；同一文件取最好的 3 段平均当文件分，见 DESIGN.md）
- [x] 混合检索与「匹配原因」输出

### 聚类
- [x] 聚类 → 整理方案依据（现为全局聚类，尚无「课程内」概念）

### 整理方案
- [~] 建议方案生成（已做归并；重命名 / 打标签未做）
- [x] 用户确认 / 拒绝
- [x] 确认后执行（v1 仅做安全操作：导出整理报告）

### 界面
- [~] app.py 入口 + ui/ 组件（入口已做；ui/ 组件目录未建，界面暂写在 app.py 里）

### 文档
- [x] README「运行方式」
- [x] 项目文档体系（README / DESIGN / DECISIONS / ROADMAP / CHANGELOG / NOTES）
- [x] 新手教程 `tutorial.md`（Python 语法，已到第九课 + 综合练习）

### 测试
- [x] pytest 覆盖核心模块（extract / chunk / classify / course / store / search / cluster / plan / index
      共 9 个；涉及向量模型的路径用假模型 / 假函数替掉，测试不联网、不下载模型）
- [x] 真实模型的端到端检索测试：做成手动脚本 `tools/benchmark.py`
      （要加载模型，不适合放进默认测试集，pytest 里仍旧用假模型替身；
      见 DESIGN.md「可复现性」）
- [x] 检索质量的回归断言：`tools/benchmark.py --check`——任一条查询的标准答案
      没排第 1 就非 0 退出。查询已从 6 条扩到 21 条，覆盖 21/22 个语料文件
      （唯一没标的是 `作业1.txt`：它按设计就是个"经常排得高、但从来不是答案"的干扰项）

