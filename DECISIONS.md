# DECISIONS —— 技术选择及理由

每条记录：**决策 → 理由**，必要时补充「备选」「影响」。格式参考 ADR（Architecture Decision Record）。

## D-001 用 Streamlit 做第一版界面
- 决策：第一版界面用 Streamlit。
- 理由：几行代码出界面，适合「先跑通核心链路」；第一版重点是验证功能而非工程化界面。
- 备选：FastAPI + 前端框架（v0.2 再用）、PyQt/Tkinter 桌面（分发重）。
- 影响：核心逻辑必须与 Streamlit 解耦，否则第二阶段重构成本高。

## D-002 核心逻辑与界面彻底分离
- 决策：核心能力做成纯 Python 包，UI 是薄壳。
- 理由：Streamlit 每次交互重跑脚本；逻辑混入 UI 会导致第二阶段难抽成 FastAPI 接口。
- 影响：核心包为纯函数 + 依赖注入，可独立测试。

## D-003 用 SQLite 存储
- 决策：持久化用 SQLite。
- 理由：单机、零运维；几百~几千文件规模完全够用；便于离线与备份。
- 备选：PostgreSQL（过重）、纯 JSON（无查询能力）。
- 影响：表结构需谨慎设计，见 DESIGN.md 数据模型。

## D-004 用 PyMuPDF 提取 PDF
- 决策：PDF 文本提取用 PyMuPDF。
- 理由：纯文本提取快且稳，中文支持好。
- 备选：pdfplumber（排版细但慢）、pdfminer。
- 影响：扫描件（无文本层）识别为「需 OCR」，v1 不处理。

## D-005 用 Sentence Transformers 中文小模型
- 决策：语义向量用 Sentence Transformers，模型定为 `BAAI/bge-small-zh-v1.5`。
- 理由：CPU 可跑、无需显卡、不接付费大模型。**这个具体型号是在 practice/ 语料上实测选出来的**，
  不是拍的：最开始用的是 `shibing624/text2vec-base-chinese`，它把相似度压缩得太厉害
  （同样卡当时那个 0.45，会放行 46% 的段落，阈值形同虚设），21 条带标准答案的查询里只有
  15 条把答案排在第一。换成 bge-small 后，同样阈值只放行 23%，**21 条全对**，
  而且**体积小 4 倍（92M vs 390M）、编码快 4 倍**。更大的 `bge-base-zh-v1.5`
  **排序反而更差**（20/21，跟旧模型栽在同一条查询上），代价却是 8 倍体积、6 倍耗时——
  **更大不等于更准**。所有数字都能用 `python tools/benchmark.py` 重跑（见 DESIGN.md）。
- 备选：`BAAI/bge-base-zh-v1.5`（更重且排序没更好）、`paraphrase-multilingual-MiniLM`（中文偏弱）。
- 影响：首次联网下载模型（约 90 MB），之后离线；模型路径固定缓存。
  换模型不用写迁移代码——`embedding_model` 对不上就会自动全部重算（见 DESIGN.md）。

## D-006 类型分类：规则打底 + 统一接口 + 后续 ML
- 决策：v1 用规则分类器，抽象统一接口，后续加 ML 实现。
- 理由：零数据冷启动；规则可解释、可让用户改；用户改标签沉淀为 ML 训练数据。
- 影响：分类器必须抽象成统一接口（TypeClassifier），ML 作为第二个实现替换。

## D-007 课程分类：半自动
- 决策：系统给候选课程建议，用户确认/修改。
- 理由：课程归属涉及用户主观组织，全自动易错；候选建议 + 确认最稳。
- 影响：需要独立的 CourseSuggester 模块。

## D-008 不自动移动/删除原文件
- 决策：导入只读，整理动作先作为 PlanItem，用户确认后才执行。
- 理由：误删/误分类不可逆，风险高。
- 影响：整理流程多一层「建议 → 确认 → 执行」。

## D-009 用 charset-normalizer 探测编码
- 决策：文本读取前自动探测编码。
- 理由：中文 Windows 下 txt 常为 GBK/GB18030，md 为 UTF-8，文件名也可能乱码。

## D-010 向量分块：文档级 + 段落级
- 决策：文档级向量服务聚类，段落级向量服务语义搜索。
- 理由：长文档整篇向量会稀释语义；分块检索更准。

## D-011 用 scikit-learn 做关键词/分类/聚类
- 决策：TF-IDF、分类、聚类用 scikit-learn。
- 理由：正统、成熟、纯 CPU，开箱即用。

## D-012 系统建议值与用户修正分开落库
- 决策：`files` 表用 `suggested_*` 列保留系统建议，最终值单独存；标签用 `status` 标记
  `active / rejected`，拒绝自动标签时改状态而非删除。
- 理由：D-006 依赖「用户改标签」沉淀 ML 训练数据；若建议值被覆盖，既无法统计分类器
  准确率，也丢失了训练样本。
- 影响：表结构略增列；写库时区分「写建议」与「写确认」两条路径。

## D-013 入口文件 app.py，界面组件目录 ui/
- 决策：Streamlit 入口固定为根目录 `app.py`，界面组件放 `ui/`。
- 理由：避免 `app.py` 与 `app/` 目录重名造成混淆。
