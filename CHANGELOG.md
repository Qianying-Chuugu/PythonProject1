# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/) 格式，版本号遵循 [SemVer](https://semver.org/)。

## [Unreleased]

### Added
- 项目初始化：确定 StudyOrganizer 的目标与第一版范围。
- 建立项目文档：README、ROADMAP、DESIGN、DECISIONS、CHANGELOG。
- 明确第一版范围：本地离线、只读导入、不移动原文件、先出建议方案。
- 新增根目录 `.gitignore`（排除 `.venv/`、`.idea/`、模型缓存、SQLite 文件等）。
- 数据模型补充：`files.suggested_*` 与 `file_tags.confidence/status`，
  用于保留系统建议与用户修正（见 DECISIONS D-012）。
- 核心包骨架 `studyorganizer/` 与 `extract.py`（extract_text 支持 .txt / .md）
- 文本提取自动检测编码（charset-normalizer），解决 GBK/UTF-8 混用
- 新增 `tutorial.md`（Python 语法教程）、`NOTES.md`（开发笔记）、`requirements.txt`
- 新增 `search.py`：语义检索（sentence-transformers 中文模型）
- 新增 `cluster.py`：层次聚类，把内容相近的文件归组
- 新增 `plan.py`：生成 / 确认 / 导出整理方案
- 新增 `app.py`：Streamlit 界面（导入 / 浏览 / 搜索 / 整理方案）
- 新增 `tests/`：pytest 覆盖 extract / classify / store
- 检索结果带「匹配原因」：标题关键词 / 正文关键词 / 语义各自解释为什么命中
- 新增正文关键词检索（TF-IDF；中文按字符 2~3 元切分，无需分词库）
- 新增混合检索 `search_hybrid`：三种方法各自归一化后加权合成总分，权重可调；
  被多个方法命中时列出多条原因
- 语义检索新增 `min_score` 相似度下限，过滤不相关结果
- 新增课程归属（半自动）：`courses` 表 + `course.py` 的 `suggest_course`
  （从文件名「课程名_内容」抽课程名）；导入时自动写 `files.suggested_course_id`
- 界面新增「课程归属」区：逐条确认 / 修改最终课程，写入 `files.course_id`
- `files` 表新增 `suggested_course_id` / `course_id` 两列，保留系统建议与用户修正（D-012）
- 向量持久化：`files` 表新增 `embedding` / `embedding_model` 两列，向量算一次存库，
  语义检索与聚类直接读，不再每次现算
- 新增 `studyorganizer/index.py`：`ensure_embeddings()`（补算漏的 / 过期重算）与
  `import_and_index()`（导入 + 建索引）
- 向量过期的两种触发，判断统一成一条查询：正文变了（`save_file` 的 upsert 自动清空）、
  换模型了（`embedding_model` 与当前模型名对不上）
- 界面导入改用 `index.import_and_index`；启动时自动补一次索引，
  升级前导入的老库不用手动重导
- 新增 `tests/test_index.py`（6 个用例，覆盖上述四种过期情况 + 导入建索引）
- 段落级向量：新增 `studyorganizer/chunk.py`（`chunk_text()` 切段）与 `chunks` 表，
  由 `index.ensure_chunk_embeddings()` 负责切段 + 补向量
- `chunks` 表结构与 `files` 表对称（`embedding` + `embedding_model`），过期判断同一套；
  正文变了则把该文件的段落整批删掉，下次重新切、重新算
- 语义检索 `search_semantic` 改为**按段落**匹配：丢掉低于 `min_score` 的段落，
  同文件取最好的 3 段平均当文件分；原因里带上命中的段号与片段
- `practice/` 新增两个长样本：`算法_动态规划长讲义.txt`（4159 字 → 22 段）、
  `操作系统_进程调度长讲义.pdf`（5 页，每页一个「自然段」→ 二次切分）
- 新增 `tests/test_chunk.py`（10 个用例）；`test_index.py` 补 5 个段落后端用例；
  `test_search.py` 补 6 个「段落合成文件分」用例
- 新增 `tools/benchmark.py`：检索实测脚本（手动跑，不进 pytest）。走真正的生产路径
  建库（`index.import_and_index`）并调**真的** `search_semantic()`，报告「每条查询的
  标准答案排第几」、段落级间距、阈值扫描、编码耗时、模型体积；`--model` 可换模型对比。
  用临时数据库（把工作目录切到临时目录，`store._DB_PATH` 是相对路径），
  **绝不碰仓库里的 `studyorganizer.db`**，跑完还会核对它的指纹
- 内置 21 条带标准答案的测试查询（原 6 条），依据逐条写在注释里——**都是打开文件
  读过后标的**。覆盖 22 个语料文件里的 21 个；唯一没标的是 `作业1.txt`，它按设计
  就是个"经常排得高、却从来不是答案"的干扰项。新增的 15 条里 4 条专门覆盖只有题目、
  没有答案的试卷/作业（标准答案填它们，意思是"该被找到的是这份卷子"，不是"它答了
  这个问题"）；其中「动态规划的状态应该怎么定义」是 NOTES 第 8 条里我标错的那条，
  早已把 4 个正确答案都填上，变成一条正规用例
- `tools/benchmark.py --check`：**回归断言**。任一条查询的标准答案没排第 1 就非 0
  退出，可以拿去卡手工步骤。失败时会同时列出"检索真的退化了"和"标准答案标错了"
  两种可能（NOTES 第 8 条），不诱导人直接去改检索代码
- 阈值扫描现在会把每条没过线的查询**名字和名次**一起打出来（原来只有 `20/21`
  这个数，不知道该查哪条）
- `QUERIES` 新增 4 条 **`"answers": []` 的语料外探针**（烹饪 / 五线谱 / 光合作用 /
  唐朝灭亡），依据是**先把 22 个语料文件全读过一遍**、确认里面确实没有答案
  （NOTES 第 8 条的规矩）。它们不参与"答案排第 1"的统计和 `--check` 断言
  （没有答案就没什么可排第 1 的），但**参与"返回几个文件 / 放行率"**——
  这正是相对阈值唯一能体现差别的地方，没有它们整个扫描量不出东西
- `tools/benchmark.py` 新增第四节**相对阈值扫描**：对 6 档 `均值 + k×σ` 和 4 档
  「分数最高前 p%」，报告四个数——答案排第几、**每条查询平均返回几个文件**
  （分成"有答案 / 语料外"两组，这是原来的旗舰指标看不见的）、放行率、
  分数线区间与**绝对地板被触发几次**；另打印分数分布曲线（用来确认 bge 的分数是
  压在一起的、没有明显拐点，以支持"不做拐点检测"这个决定）。
  **量相对阈值不需要改生产代码**——把算出来的线当 `min_score=` 传进去，
  走的还是真的 `search_semantic`
- `tools/benchmark.py` 新增第五节**合成规则扫描**：量「过了线之后文件分怎么合成」
  （现状 = 每个文件最好的前 3 段求平均，`search._TOP_N = 3`，这个 3 从来没被量过）。
  扫 7 条规则——前 2/3/5 段平均、只取最高分、全部过线段平均、衰减加权 w=0.7/0.5。
  报告「排第 1 的查询数」和**新指标「最小领先」**（答案分 − 最高的**非答案**文件分）。
  **这一节一次模型都不用调**：段落分数上面已经算好，本地按文件分组换公式即可
  （过滤是按全局标量切的，所以"先过滤再取前 3"≡"先取前 3 再过滤"）。
  配一行**量具自检**：本地重算 vs 真的 `search_semantic`，最大差 0.000000
- `tools/benchmark.py` 新增第六节**二维扫描（`min_score` × 合成规则）**：
  因为 `min_score` 同时是过滤器和"取前 N 段的取材范围"（NOTES 第 11 条），
  固定一维去改另一维量出来的结论不算数。三张表：排第 1 数 / 最小领先 /
  **平均返回文件数**（表 C 是这次的关键，见下）。纯本地计算，同样不过模型
- 新增「拆开看」输出：把最薄的那条查询和「0-1 背包」那条的**候选文件逐段分数**
  打出来（每个文件列全部过线段落的分数、前 3 段平均、只取最高、全部平均）
- 用它对三个模型做了完整对照（见 DESIGN.md）：旧模型 15/21、bge-small 21/21、
  bge-base **20/21**
- **标签系统**：新增 `tags` / `file_tags` 两张表（`DESIGN.md` 里早就画好、一直标着
  「尚未实现」的那两张），补上 v0.1 闭环里最后没做的一环「分类打标签」
  - `store` 新增 4 个函数，跟课程那 4 个一一对应：`get_or_create_tag` / `list_tags` /
    `list_file_tags` / `set_file_tags`，另加一个只给自动标签用的 `add_auto_tag`
  - 导入时自动打标签：分类器判出的类型顺手记一条（`source='auto'`）；
    判成「未知」不记，一个叫「未知」的标签是纯噪音
  - **用户去掉自动标签 = 改 `status` 为 `rejected`，行留着**（D-012 要求拒绝留痕）；
    去掉自己手输的标签 = 整行删掉。见 DESIGN.md「标签的三态」
  - **自动标签不会被重新导入激活**：`add_auto_tag` 走 `INSERT OR IGNORE`，
    命中已有记录（含被拒的）就什么都不做，既不激活也不改 `source`
  - 界面新增「标签」区：每个文件一个 `st.multiselect`（本仓库第一个），
    `accept_new_options=True` 支持直接手输新标签；被拒过的自动标签用 `st.caption`
    列出来，免得拒完就再也想不起来
  - 「文件库」新增按标签筛选（取并集，适合浏览），表格里也多一列标签
- `plan.export_report` 修好格式：以前把 `action` 和 `reason` 全丢掉、硬写「## 组N」，
  现在是「## N. 动作」+ 一行原因——用户在报告里终于看得到"为什么建议这么干"
- `store.save_file` 现在返回 `file_id`（导入时要拿它挂自动标签）。原来返回 `None`、
  调用方都不接，属于向后兼容的改动
- 新增 `tests/test_tags.py`（11 个用例，用 tmp_path 临时真库）：标签三态、
  标签名清洗、同名标签不重复建，以及**被拒的自动标签不能被重新导入激活**；
  `test_plan.py` 补 2 条（导出报告的格式、generate_plan 不该去扫 files 表）

### Removed
- 整理方案里曾短暂加过一条**「打标签」建议**（按「课程 + 类型」元数据分组，
  提醒用户给同类文件打标签），**已删除**。两个方向都不成立：把课程名当标签等于让用户
  把课程归属记两遍（课程已有自己的表和确认流程，见 D-014）；去掉课程名只按类型分组，
  那又跟导入时自动打的类型标签重复。标签这条路就是**导入时自动检测 + 界面手动改**。

### Fixed
- **「课程归属」和「整理方案」的按钮 key 会撞车，撞上就整块渲染不出来。**
  两处都用 `key=f"ok_{id}"`，而**文件 id 和方案条目 id 是两套独立的数字、都从 1 开始**
  ——某份还没确认课程的文件（id=N）碰上一条 pending 方案条目（id=N）时，Streamlit
  直接抛 `StreamlitDuplicateElementKey: There are multiple elements with the same
  key='ok_1'`，两块内容一起消失。这是加标签系统时顺手发现的**旧问题**（不是标签引入的），
  已用最小例子复现后修掉：课程那边改成 `course_ok_{fid}`，两个命名空间分开。
- **文档里一句"当结论写、其实从没量过"的话是错的。** README / ROADMAP / DESIGN 都写着
  「三个刚过线的错段落平均起来，赢过一段很准的对段落」。拆开量了，两处都对不上：
  对文件有 **20 段**过线、前三段 0.750/0.744/0.719，不是"一段"；错文件那三段是
  0.766/0.677/0.650，只有第一个是尖峰，另两个离 0.45 那条线远得很，不是"刚过线"。
  结论（平均救了它）是对的，机制描述是错的——当时是**推断**，现在量了才发现。
  改成实测的说法：**尖峰 vs 深度**
- **`tools/benchmark.py` 第五节新写的「最小领先」指标，第一版定义就是错的。**
  按「第 1 名 − 第 2 名」算，跑出来"最薄的是动态规划和贪心"——拆开看才发现那条
  查询有**三份标准答案**，第 2 名也是正确答案，量的是两个正确答案之间的差距。
  改成对着**最高的非答案文件**量。跟正文里那条「写向量时查了、读向量时忘了查」
  同类：量具的定义本身也会错，而且错了不会报错，只会印出一个像模像样的数
- **`tools/benchmark.py` 里自己抄了一份"当前默认阈值"（`0.45`），会和 `search.py`
  里真正的默认值各走各的。** 哪天把 `search_semantic` 的默认值改了，报告还会印着旧
  的数说「<- 当前默认」，报告本身就是假的——跟 NOTES 第 10 条同一类错（错的是量具）。
  现在改成从签名里读：`inspect.signature(search.search_semantic).parameters["min_score"].default`，
  并且把它并进 `THRESHOLDS`（不再单独写死在列表里）。
  变异测试验证过：把 `search.py` 的默认值临时改成 `0.50`，脚本读到 `0.5`、
  `THRESHOLDS` 里的 `0.45` 自动换成 `0.50`；改回来又跟回 `0.45`。
  （**2026-09 补**：这条后来收紧了——"插入当前默认值"意味着 `0.45` 只是以"默认值"
  这一个身份存在，默认值一改成 0.50，`0.45` 就整列从扫描表里消失、文档里的对照表
  没法复现。现在 `0.45` 写死在 `阈值地标` 里。见下面 Changed 里那条。）
- **向量读取没有核对 `embedding_model`，会读出别的模型算的向量。**
  `list_file_vectors` / `list_chunk_vectors` 原先只查 `embedding IS NOT NULL`。
  正常路径上不会出事（`app.py` 启动时先跑完 `ensure_*` 才渲染搜索框），但换模型
  要把全库重算几十秒，用户等不及 Ctrl-C 中断后，库里会混着两种模型的向量
  （旧 768 维 / 新 512 维），`np.vstack` 直接崩
  `ValueError: Incompatible dimension for X and Y matrices`。
  现在两个读函数都带 `AND embedding_model IS ?`，只返回当前模型的向量，
  口径和 `list_*_needing_embedding` 完全一致（写和读用同一个判断）。
  `model_name` 是**必填参数**，从 `search` / `plan → cluster` 一路透传，
  `app.py` 传 `search.MODEL_NAME`——忘了传会当场报错，不会悄悄退化成不过滤。
  实测：中断状态下 `search_semantic` 不再崩，返回 0 条（暂时读不到），
  `cluster_files` 正常，补跑 `ensure_chunk_embeddings()` 后检索结果与之前完全一致。
  新增 `test_读向量时只认当前模型`（并用变异测试确认它不是空测试）。
- 语义分数是 numpy `float32`，直接 `round` 后打印成长小数
  （如 `0.5419999957084656`），显示前先转 `float`
- `save_file` 由 `INSERT OR REPLACE` 改为 `INSERT ... ON CONFLICT(path) DO UPDATE`：
  原先重复导入会把用户确认的 `course_id` 抹成 NULL
- `list_files_without_chunks` 原先用 SQL 的 `trim(text) != ''` 判断正文是否为空，
  但 **SQLite 的 `trim()` 只去空格、不去换行**，导致「正文只有几个换行」的文件
  每次都被当成没切过、反复重切（切又切不出段，永远轮不到它被标记成已处理）。
  判断挪到 Python 里用 `str.strip()`
- `tools/benchmark.py` 的「语料外平均返回文件数」原先印一位小数。语料外只有 4 条探针，
  平均值必然是 0.25 的倍数，而 `1/4` 会被 round-half-even 显示成 **`0.2`**——比真值
  还小。偏偏这行就是专门用来看"结果有没有被删光"的，四舍五入正好把那个唯一的非零值
  抹掉一半。改成印两位小数（`1.75` → `0.25`）
- `tools/benchmark.py` 第六节里写死的两个数字改成从数据里算：「读法」那段原先写
  「（现在 10.9 个）」，默认值一改就成了假话，现在按当前门槛实算；
  「绝对 0.45（现状）」那行也统一成两位小数，免得同一张表里 `0.5` 和 `0.50` 混着印

### Changed
- 移出第一版范围：相似文件检测（含字节哈希 / SimHash / 语义相似三层去重）。
- 删除 README「第一版暂不做」小节。
- 界面结构命名统一：入口 `app.py` + 组件目录 `ui/`（原 `app/` 易混淆，见 D-013）。
- `init_db` 对已存在的老库自动 `ALTER TABLE` 补列（幂等，可反复调用）。
- 语义检索 `search_semantic` 与聚类 `cluster_files` 改为读库里的向量，不再现算；
  `cluster_files` 因此不再依赖向量模型。
- `search.py` 的 `_get_model` / `_MODEL_NAME` 改为公开的 `get_model` / `MODEL_NAME`，
  供 `index.py` 复用（私有名字跨模块用不合适）。
- `requirements.txt` 补上 `numpy`（`search.py` / `cluster.py` / `index.py` 直接用它）。
- `search_semantic` 的 `min_score` 默认值由 `0.3` 提到 `0.45`。这是**实测校准**的结果：
  在 `practice/` 语料上，`0.3` 会让 99% 的段落过关、等于没过滤；`0.45` 能砍掉明显
  跑题的尾部（详见 DESIGN.md「检索结果怎么从段落合成到文件」）。
- `app.py` 启动时同时补文档向量和段落向量，导入走 `index.import_and_index`（不变）。
- **语义向量模型换成 `BAAI/bge-small-zh-v1.5`**（原 `shibing624/text2vec-base-chinese`）。
  在 `practice/` 语料（22 文件 141 段）上实测：21 条带标准答案的查询里，旧模型只有
  15 条把答案排在第一（最惨的「最大似然估计怎么估计参数」掉到**第 11**），新模型
  21 条全对；同样卡 0.45 的放行率从 46% 降到 23%。附带好处：体积 92 MB vs 390 MB、
  编码 141 段 1.2s vs 4.9s（都小/快了 4 倍）。评测过 `bge-base-zh-v1.5`
  （放行率能压到 7%，但**排序反而更差**——20/21，跟旧模型栽在同一条 Dijkstra/Floyd
  上，却要 8 倍体积、6 倍耗时）和 BGE 的查询前缀（实测只是把所有分数统一往下推，
  间距没变好，故未采用），详见 DESIGN.md。
  换模型**没有写任何迁移代码**——`embedding_model` 与当前模型名对不上，
  `ensure_*` 就把文档向量和段落向量全部重算（已实测验证）。
- 上面这些数字原先出自用完就删的一次性脚本，**谁也复现不了**；现在全部改由
  `tools/benchmark.py` 产出，并写明指标定义。原先的「74% / 31%」是拿 3 条查询按
  "每条平均"算的、定义没写下来，重跑对不上（旧模型实际是 46%），所以整表换口径，
  并补上更有说服力的「排第 1 的查询数」一列。DESIGN.md 的「间距」说法也跟着改了：
  原先写"合成到文件级会从 +0.11~+0.13 磨到 +0.05~+0.07"，实测其实**并不一致**——
  「动态规划和贪心有什么区别」+0.243 → +0.110，而「0-1 背包问题的状态转移方程
  是什么」反而从 -0.016 升到 +0.040，改成如实描述，并点明这个余量不能当质量指标用
- **`min_score` 不是一个"越高越严"的旋钮**（21 条查询实测）：0.45~0.60 都是 21/21，
  调到 0.65 崩到 17/21（四条查询的答案段落被整条砍掉、直接未上榜）；
  **调低到 0.30 / 0.40 反而掉到 20/21**——「过拟合怎么解决」被顶到第 2。原因是
  `min_score` 不只过滤，它还决定"前 3 段平均"从哪些段里取，放宽会把文件分拖下来。
  阈值同时是过滤器和评分口径的一部分。文档原先只说"调高不会更准"，现在补上另一半
- **`_TOP_N` 保持 `3`，「前 3 段平均」这个合成规则不改。** 它跟 `min_score` 一样
  从来没被量过。固定 `0.45` 时「前 5 段平均」确实满足事先定好的标准
  （排第 1 仍是 21/21，最小领先 0.007 → 0.023），**但二维扫描一摆出来就翻了**：
  `前 3 @ 0.60 = 0.040` **高于** `前 5 @ 0.60 = 0.028`——前 5 只是在"阈值偏低、
  放进太多平庸段落"时的补救，阈值调对了就不需要。**`_TOP_N` 不是薄弱环节，阈值才是。**
  「只取最高分」20/21（输的正是「0-1 背包」那条）、「全部过线段平均」18/21，当场淘汰。
  `search.py` 一行没改
- **新指标「最小领先」自己有个盲点，靠第三张表才兜住。** 它看名次不看分母，
  所以「阈值 0.60」那格看起来白捡（最小领先 0.040，6.1 倍），实际是靠把
  「有答案」的查询平均压到 **1.6 个文件**（现状 10.9 个）换来的——**把结果删光，
  第 1 名当然领先**。这跟上一轮「答案排第几看不见列表变短」是同一类错，
  换个指标又犯一次（NOTES 第 13 条）。所以二维扫描加了**表 C：平均返回文件数**，
  并写死一条读法：比较门槛时先看"有答案"那行还在不在 `top_k`（默认 5）附近
- **`min_score` 保持在 `0.45`，不改相对阈值。** ROADMAP 上那条待办（"改成只留明显高于
  全库平均的段落"）**先量再改**，量完的结论是**不改**：相对线在"答案排第 1"上全是
  打平，在"语料外返回几个文件"上**全都更差**（现状 1.8 个，相对线最好也要 5.0 个；
  摆平有答案返回数再比差 6~8 倍）。原因是相对线衡量的是「这条查询里谁比谁高」，
  不是「谁够格」——一条跑题的查询分数照样有高有低，相对线照样砍一半留一半，
  **永远不可能返回空**。`search.py` 一行没改，完整数据进 DESIGN.md，
  教训进 NOTES.md 第 12 条；ROADMAP 那条从"待办"改写成"有结论了"（含结论是不做），
  并把真正还没解决的方向（改「前 3 段平均」的合成规则）单列成新待办
- README（中英）同步：模型对照、相对阈值的举例数字、「端到端测试」那条都换成
  可复现的说法，项目结构树补上 `tools/`
- **`search_semantic` 的 `min_score` 默认值由 `0.45` 提到 `0.50`。** 这是本项目第一次
  **量完了、数据支持改、于是真改了**的调参（前两轮量完的结论都是不改）。支撑数据
  （DESIGN.md 表 A/表 B/表 C）：
  - 答案排第 1 仍是 **21/21**；
  - 最小领先 0.007 → **0.015**（最薄那条厚了一倍）；
  - **语料外查询平均返回文件数 1.75 → 0.25**（4 条探针里 3 条返回空文件）——这正是
    上一轮改相对阈值想拿却没拿到的效果，换个门槛就白捡了；
  - 「有答案」查询平均返回 10.9 → 5.0 个文件，但 `search_semantic` 默认 `top_k = 5`，
    **用户可见的结果一条没少**（这正是表 C 存在的意义：让"列表缩水"看得见）；
  - 再往上加就不行：0.55 → 2.3 个文件、0.60 → 1.6，开始真的少于 `top_k = 5`；
    0.65 崩到 17/21。而 0.30 / 0.40 反而掉到 20/21（门槛同时是"前 3 段平均"的取材
    范围，放大会把文件分拖下来，NOTES 第 11 条）。

  `search.py` **只改了这一处默认值**，`_TOP_N` 仍是 3。改动本身没有弄挂任何测试
  （58 passed）——但这**不是好消息**：测试数据里没有落在 0.45~0.50 之间的分数，
  所以"默认值是哪个"对测试集完全不可见，等于这个数一直没人看着。README（中英）里
  那条也从「还没做」挪进了已完成的能力清单
- `tools/benchmark.py` 的**阈值扫描列表改成"写死的地标值 + 当前默认值"**。原先写的是
  「几个写死的数 + 插入当前默认值」，而 0.45 只在"当前默认值"这个身份里出现过——
  本轮把默认值一改，**0.45 就整列从第五、六节的三张表里静悄悄消失了**，DESIGN.md
  里那张"改之前 vs 改之后"的对照表当场没法复现。这跟 NOTES 第 10 条（量具自己出错）
  是同一类：量具跟着被测对象一起动了，谁也没发现。现在 0.45 写死在 `阈值地标` 里。
  顺带把第五、六节各抄一份的门槛字面量合并成同一份
